name: CodeQL

on:
  schedule:
    - cron: '15 0 * * 0'

jobs:
  codeql:
    uses: scottobert/deployment-pipeline/.github/workflows/codeql.yml@main
    permissions:
      actions: read
      contents: read
      security-events: write
